<UserControl x:Class="ViewLayerWPF.GameWindowControls.PlayerTokenControl"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             mc:Ignorable="d" 
             xmlns:local="clr-namespace:ViewLayerWPF.ValueConverters"
             d:DesignHeight="1000" d:DesignWidth="1000">
    <Control.Resources>
        <local:TokenRowToYPosition x:Key="RowToYPos" />
        <local:TokenColToXPosition x:Key="ColToXPos" />
        <local:BoolToVisibility x:Key="BoolToVisibility" />
    </Control.Resources>

    <Canvas Name="TokenCanvas">
        <Ellipse Name="PlayerToken" Visibility="{Binding Path=IsInTheGame, Converter={StaticResource BoolToVisibility}}"
                 Height="25" Width="23" Stroke="White" StrokeThickness="1" Opacity="0.95"></Ellipse>
    </Canvas>
</UserControl>
