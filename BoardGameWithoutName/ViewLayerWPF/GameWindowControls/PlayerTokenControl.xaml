<UserControl x:Class="ViewLayerWPF.GameWindowControls.PlayerTokenControl"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             mc:Ignorable="d" 
             xmlns:local="clr-namespace:ViewLayerWPF.ValueConverters"
             d:DesignHeight="1000" d:DesignWidth="1000">
    <Control.Resources>
        <local:TokenRowToYPosition x:Key="RowToYPos" />
        <local:TokenColToXPosition x:Key="ColToXPos" />
    </Control.Resources>

    <Canvas Name="TokenCanvas" SizeChanged="TokenCanvas_SizeChanged" Loaded="TokenCanvas_Loaded">
        <Ellipse Name="PlayerToken"  
                 Canvas.Top="{Binding Path = Field.Row, Converter={StaticResource RowToYPos}}"
                 Canvas.Left="{Binding Path = Field.Column, Converter={StaticResource ColToXPos}}"
                 Height="20" Width="30" Stroke="White" StrokeThickness="3"></Ellipse>
    </Canvas>
</UserControl>
